# Use empty base URL in dev (Vite proxies /api to the backend).
# For production builds, set the full API origin (no trailing slash).
VITE_API_BASE_URL=

# Backend origin used only by the Vite dev-server proxy.
VITE_API_PROXY_TARGET=http://localhost:8080